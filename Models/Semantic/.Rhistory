yearlycount
yearlycount=read.csv(file='data/patentcount_window5.0.csv',sep=";",header=TRUE,stringsAsFactors = FALSE)
yearlycount
yearrange
yearlycount[yearlycount$yearrange==yearrange,2]
for(year in wyears){
#year=1980
yearrange = paste0((year-windowSize+1),"-",year)
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
show(mean(argmaxs$argmaxeth)/yearlycount[yearlycount$yearrange==yearrange,2])
#
# g=ggplot(sensdata[sensdata$dispth>0.06,] %>% group_by(dispth,eth) %>% summarise(maxmod=max(modularity),comnum=min(comnum),vcount=mean(vcount)))
# g+geom_line(aes(x=eth,y=maxmod,colour=dispth,group=dispth))
# g+geom_point(aes(x=maxmod,y=vcount,colour=dispth))
# g+geom_point(aes(x=vcount,y=maxmod,colour=dispth))
# g+geom_point(aes(x=vcount,y=comnum,colour=dispth))
# TODO : for each year, plot triobjective, highlight chosen points
}
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
show(mean(argmaxs$argmaxeth)/yearlycount[yearlycount$yearrange==yearrange,2])
#
# g=ggplot(sensdata[sensdata$dispth>0.06,] %>% group_by(dispth,eth) %>% summarise(maxmod=max(modularity),comnum=min(comnum),vcount=mean(vcount)))
# g+geom_line(aes(x=eth,y=maxmod,colour=dispth,group=dispth))
# g+geom_point(aes(x=maxmod,y=vcount,colour=dispth))
# g+geom_point(aes(x=vcount,y=maxmod,colour=dispth))
# g+geom_point(aes(x=vcount,y=comnum,colour=dispth))
# TODO : for each year, plot triobjective, highlight chosen points
}
argmax=c()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/yearlycount[yearlycount$yearrange==yearrange,2])
#
# g=ggplot(sensdata[sensdata$dispth>0.06,] %>% group_by(dispth,eth) %>% summarise(maxmod=max(modularity),comnum=min(comnum),vcount=mean(vcount)))
# g+geom_line(aes(x=eth,y=maxmod,colour=dispth,group=dispth))
# g+geom_point(aes(x=maxmod,y=vcount,colour=dispth))
# g+geom_point(aes(x=vcount,y=maxmod,colour=dispth))
# g+geom_point(aes(x=vcount,y=comnum,colour=dispth))
# TODO : for each year, plot triobjective, highlight chosen points
}
mean(argmax)
argmax=c()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
argmaxs=sensdata[sensdata$dispth>0.04&sensdata$dispth<0.08,]%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/yearlycount[yearlycount$yearrange==yearrange,2])
#
# g=ggplot(sensdata[sensdata$dispth>0.06,] %>% group_by(dispth,eth) %>% summarise(maxmod=max(modularity),comnum=min(comnum),vcount=mean(vcount)))
# g+geom_line(aes(x=eth,y=maxmod,colour=dispth,group=dispth))
# g+geom_point(aes(x=maxmod,y=vcount,colour=dispth))
# g+geom_point(aes(x=vcount,y=maxmod,colour=dispth))
# g+geom_point(aes(x=vcount,y=comnum,colour=dispth))
# TODO : for each year, plot triobjective, highlight chosen points
}
mean(argmax)
plot(argmax,type='l')
plot(wyears,argmax,type='l')
argmax=c()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
#argmaxs=sensdata[sensdata$dispth>0.04&sensdata$dispth<0.08,]
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/yearlycount[yearlycount$yearrange==yearrange,2])
#
# g=ggplot(sensdata[sensdata$dispth>0.06,] %>% group_by(dispth,eth) %>% summarise(maxmod=max(modularity),comnum=min(comnum),vcount=mean(vcount)))
# g+geom_line(aes(x=eth,y=maxmod,colour=dispth,group=dispth))
# g+geom_point(aes(x=maxmod,y=vcount,colour=dispth))
# g+geom_point(aes(x=vcount,y=maxmod,colour=dispth))
# g+geom_point(aes(x=vcount,y=comnum,colour=dispth))
# TODO : for each year, plot triobjective, highlight chosen points
}
plot(wyears,argmax,type='l')
plot(wyears,argmax,type='l')
argmax=c()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
argmaxs=sensdata[sensdata$dispth>0.05,]%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/yearlycount[yearlycount$yearrange==yearrange,2])
#
# g=ggplot(sensdata[sensdata$dispth>0.06,] %>% group_by(dispth,eth) %>% summarise(maxmod=max(modularity),comnum=min(comnum),vcount=mean(vcount)))
# g+geom_line(aes(x=eth,y=maxmod,colour=dispth,group=dispth))
# g+geom_point(aes(x=maxmod,y=vcount,colour=dispth))
# g+geom_point(aes(x=vcount,y=maxmod,colour=dispth))
# g+geom_point(aes(x=vcount,y=comnum,colour=dispth))
# TODO : for each year, plot triobjective, highlight chosen points
}
mean(argmax)
plot(wyears,argmax,type='l')
argmax=c()
alldata=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
argmaxs=sensdata[sensdata$dispth>0.05,]%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/yearlycount[yearlycount$yearrange==yearrange,2])
alldata=rbind(alldata,cbind(sensdata,year=rep(as.character(year),nrow(sensdata)),dist=abs(sensdata$dispth-mean(argmaxs$argmaxeth))/yearlycount[yearlycount$yearrange==yearrange,2]))
}
alldata
colnames(alldata)
g=ggplot(alldata)
g+geom_point(aes(x=vcount,y=comnum,colour=dist))+facet_wrap(~year)
argmax=c()
alldata=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/yearlycount[yearlycount$yearrange==yearrange,2])
alldata=rbind(alldata,cbind(sensdata,year=rep(as.character(year),nrow(sensdata)),dist=abs(sensdata$dispth-mean(argmaxs$argmaxeth))))#/yearlycount[yearlycount$yearrange==yearrange,2]))
}
g=ggplot(alldata)
g+geom_point(aes(x=vcount,y=comnum,colour=dist))+facet_wrap(~year)
argmaxs
argmax=c()
alldata=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/yearlycount[yearlycount$yearrange==yearrange,2])
alldata=rbind(alldata,cbind(sensdata,year=rep(as.character(year),nrow(sensdata)),dist=abs(sensdata$eth-mean(argmaxs$argmaxeth))))#/yearlycount[yearlycount$yearrange==yearrange,2]))
}
g=ggplot(alldata)
g+geom_point(aes(x=vcount,y=comnum,colour=dist))+facet_wrap(~year)
argmaxs
g=ggplot(alldata)
g+geom_point(aes(x=vcount,y=comnum,colour=eth))+facet_wrap(~year)
argmax=c()
alldata=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
argmaxs=sensdata[sensdata$eth<60,]%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/yearlycount[yearlycount$yearrange==yearrange,2])
alldata=rbind(alldata,cbind(sensdata,year=rep(as.character(year),nrow(sensdata)),dist=abs(sensdata$eth-mean(argmaxs$argmaxeth))))#/yearlycount[yearlycount$yearrange==yearrange,2]))
}
g=ggplot(alldata)
g+geom_point(aes(x=vcount,y=comnum,colour=eth))+facet_wrap(~year)
argmax=c()
alldata=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
sensdata=sensdata[sensdata$eth<60,]
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/yearlycount[yearlycount$yearrange==yearrange,2])
alldata=rbind(alldata,cbind(sensdata,year=rep(as.character(year),nrow(sensdata)),dist=abs(sensdata$eth-mean(argmaxs$argmaxeth))))#/yearlycount[yearlycount$yearrange==yearrange,2]))
}
g=ggplot(alldata)
g+geom_point(aes(x=vcount,y=comnum,colour=eth))+facet_wrap(~year)
g+geom_point(aes(x=comnum,y=vcount,colour=eth))+facet_wrap(~year)
g=ggplot(cbind(alldata,col=as.character(alldata$dispth==0.06)))
g+geom_point(aes(x=comnum,y=vcount,colour=col))+facet_wrap(~year)
ethunit=4.1e-5
argmax=c()
alldata=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
sensdata=sensdata[sensdata$eth<60,]
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=log((ethunit*npatents) - sensdata$eth)
)
)
}
warnings
warnings()
ethunit=4.1e-5
argmax=c()
alldata=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
sensdata=sensdata[sensdata$eth<60,]
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=log(1+(ethunit*npatents) - sensdata$eth)
)
)
}
warning()
warnings()
warnings()
ethunit=4.1e-5
argmax=c()
alldata=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
sensdata=sensdata[sensdata$eth<60,]
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=log(1+(ethunit*npatents) - sensdata$eth)
)
)
}
warnings()
ethunit=4.1e-5
argmax=c()
alldata=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
sensdata=sensdata[sensdata$eth<60,]
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=log(1+abs((ethunit*npatents) - sensdata$eth))
)
)
}
g=ggplot(alldata)
g+geom_point(aes(x=comnum,y=vcount,colour=ethunitdist))+facet_wrap(~year)
ethunit=4.1e-5
argmax=c()
alldata=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
sensdata=sensdata[sensdata$eth<60,]
sensdata = sensdata %>% group_by(dispth,eth) %>% summarise(modularity=max(modularity),comnum=min(comnum),vcount=mean(vcount))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=log(1+abs((ethunit*npatents) - sensdata$eth))
)
)
}
g=ggplot(alldata)
g+geom_point(aes(x=comnum,y=vcount,colour=ethunitdist))+facet_wrap(~year)
g=ggplot(alldata)
g+geom_point(aes(x=modularity,y=vcount,colour=ethunitdist))+facet_wrap(~year)
ethunit=4.1e-5
# -> mean argmax on all dispth ; lay on pareto front (comnum,vcount) (or very near) for each year
argmax=c()
alldata=data.frame()
proj=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
#sensdata=sensdata[sensdata$eth<60,]
sensdata = sensdata %>% group_by(dispth,eth) %>% summarise(modularity=max(modularity),comnum=min(comnum),vcount=mean(vcount))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
ethunitdist = log(1+abs((ethunit*npatents) - sensdata$eth))
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=ethunitdist
)
)
projrows = which(ethunitdist==min(ethunitdist))
proj=rbind(proj,cbind(
val=c(sensdata$comnum[projrows],sensdata$vcount[projrows]),
type=c(rep("number of communities",length(projrows)),rep("number of vertices",length(projrows))),
dispth=c(sensdata$dispth[projrows],sensdata$dispth[projrows])
))
}
g=ggplot(proj)
g+geom_line(aes(x=dispth,y=val,col=type,group=type))+facet_wrap(~year)
ethunit=4.1e-5
# -> mean argmax on all dispth ; lay on pareto front (comnum,vcount) (or very near) for each year
argmax=c()
alldata=data.frame()
proj=data.frame()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
#sensdata=sensdata[sensdata$eth<60,]
sensdata = sensdata %>% group_by(dispth,eth) %>% summarise(modularity=max(modularity),comnum=min(comnum),vcount=mean(vcount))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
ethunitdist = log(1+abs((ethunit*npatents) - sensdata$eth))
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=ethunitdist
)
)
projrows = which(ethunitdist==min(ethunitdist))
proj=rbind(proj,cbind(
val=c(sensdata$comnum[projrows],sensdata$vcount[projrows]),
type=c(rep("number of communities",length(projrows)),rep("number of vertices",length(projrows))),
dispth=c(sensdata$dispth[projrows],sensdata$dispth[projrows]),
year=rep(as.character(year),2*length(projrows))
))
}
g=ggplot(proj)
g+geom_line(aes(x=dispth,y=val,col=type,group=type))+facet_wrap(~year)
ethunit=4.1e-5
# -> mean argmax on all dispth ; lay on pareto front (comnum,vcount) (or very near) for each year
argmax=c()
alldata=data.frame()
projval=c();projtype=c();projdisp=c();projyear=c()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
#sensdata=sensdata[sensdata$eth<60,]
sensdata = sensdata %>% group_by(dispth,eth) %>% summarise(modularity=max(modularity),comnum=min(comnum),vcount=mean(vcount))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
ethunitdist = log(1+abs((ethunit*npatents) - sensdata$eth))
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=ethunitdist
)
)
projrows = which(ethunitdist==min(ethunitdist))
projval=append(projval,c(sensdata$comnum[projrows],sensdata$vcount[projrows]))
projtype=append(projtype,c(rep("number of communities",length(projrows)),rep("number of vertices",length(projrows))))
projdisp=append(projdisp,c(sensdata$dispth[projrows],sensdata$dispth[projrows]))
projyear=append(projyear,rep(as.character(year),2*length(projrows)))
}
g=ggplot(data.frame(dispth=projdisp,val=projval,type=projtype,year=projyear))
g+geom_line(aes(x=dispth,y=val,col=type,group=type))+facet_wrap(~year)
ethunit=4.1e-5
# -> mean argmax on all dispth ; lay on pareto front (comnum,vcount) (or very near) for each year
argmax=c()
alldata=data.frame()
projval=c();projtype=c();projdisp=c();projyear=c()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
#sensdata=sensdata[sensdata$eth<60,]
sensdata = sensdata %>% group_by(dispth,eth) %>% summarise(modularity=max(modularity),comnum=min(comnum),vcount=mean(vcount))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
ethunitdist = log(1+abs((ethunit*npatents) - sensdata$eth))
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=ethunitdist
)
)
projrows = which(ethunitdist==min(ethunitdist))
projval=append(projval,c(sensdata$comnum[projrows]/max(sensdata$comnum[projrows]),sensdata$vcount[projrows]/sensdata$vcount[projrows]))
projtype=append(projtype,c(rep("number of communities",length(projrows)),rep("number of vertices",length(projrows))))
projdisp=append(projdisp,c(sensdata$dispth[projrows],sensdata$dispth[projrows]))
projyear=append(projyear,rep(as.character(year),2*length(projrows)))
}
g=ggplot(data.frame(dispth=projdisp,val=projval,type=projtype,year=projyear))
g+geom_line(aes(x=dispth,y=val,col=type,group=type))+facet_wrap(~year)
ethunit=4.1e-5
# -> mean argmax on all dispth ; lay on pareto front (comnum,vcount) (or very near) for each year
argmax=c()
alldata=data.frame()
projval=c();projtype=c();projdisp=c();projyear=c()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
#sensdata=sensdata[sensdata$eth<60,]
sensdata = sensdata %>% group_by(dispth,eth) %>% summarise(modularity=max(modularity),comnum=min(comnum),vcount=mean(vcount))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
ethunitdist = log(1+abs((ethunit*npatents) - sensdata$eth))
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=ethunitdist
)
)
projrows = which(ethunitdist==min(ethunitdist))
projval=append(projval,c(sensdata$comnum[projrows]/max(sensdata$comnum[projrows]),sensdata$vcount[projrows]/max(sensdata$vcount[projrows])))
projtype=append(projtype,c(rep("number of communities",length(projrows)),rep("number of vertices",length(projrows))))
projdisp=append(projdisp,c(sensdata$dispth[projrows],sensdata$dispth[projrows]))
projyear=append(projyear,rep(as.character(year),2*length(projrows)))
}
g=ggplot(data.frame(dispth=projdisp,val=projval,type=projtype,year=projyear))
g+geom_line(aes(x=dispth,y=val,col=type,group=type))+facet_wrap(~year)
rep(1:3,3)
ethunit=4.1e-5
# -> mean argmax on all dispth ; lay on pareto front (comnum,vcount) (or very near) for each year
argmax=c()
alldata=data.frame()
projval=c();projtype=c();projdisp=c();projyear=c()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
#sensdata=sensdata[sensdata$eth<60,]
sensdata = sensdata %>% group_by(dispth,eth) %>% summarise(modularity=max(modularity),comnum=min(comnum),vcount=mean(vcount))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
ethunitdist = log(1+abs((ethunit*npatents) - sensdata$eth))
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=ethunitdist
)
)
projrows = which(ethunitdist==min(ethunitdist))
projval=append(projval,c(sensdata$comnum[projrows]/max(sensdata$comnum[projrows]),sensdata$vcount[projrows]/max(sensdata$vcount[projrows]),sensdata$modularity[projrows]/max(sensdata$modularity[projrows])))
projtype=append(projtype,c(rep("number of communities",length(projrows)),rep("number of vertices",length(projrows)),rep("modularity",length(projrows))))
projdisp=append(projdisp,re(sensdata$dispth[projrows],3))
projyear=append(projyear,rep(as.character(year),3*length(projrows)))
}
ethunit=4.1e-5
# -> mean argmax on all dispth ; lay on pareto front (comnum,vcount) (or very near) for each year
argmax=c()
alldata=data.frame()
projval=c();projtype=c();projdisp=c();projyear=c()
for(year in wyears){
#year=1980
show(year)
yearrange = paste0((year-windowSize+1),"-",year)
npatents=yearlycount[yearlycount$yearrange==yearrange,2]
sensdata = as.tbl(read.csv(file=paste0('sensitivity/sensitivity_',yearrange,"_",kwLimit,"_eth",eth,".csv"),sep=";",header=TRUE))
#sensdata=sensdata[sensdata$eth<60,]
sensdata = sensdata %>% group_by(dispth,eth) %>% summarise(modularity=max(modularity),comnum=min(comnum),vcount=mean(vcount))
argmaxs=sensdata%>% group_by(dispth)%>%summarise(argmaxeth=eth[comnum==max(comnum)][1])
argmax=append(argmax,mean(argmaxs$argmaxeth)/npatents)
ethunitdist = log(1+abs((ethunit*npatents) - sensdata$eth))
alldata=rbind(alldata,cbind(sensdata,
year=rep(as.character(year),nrow(sensdata)),
dist=abs(sensdata$eth-mean(argmaxs$argmaxeth)),
ethunitdist=ethunitdist
)
)
projrows = which(ethunitdist==min(ethunitdist))
projval=append(projval,c(sensdata$comnum[projrows]/max(sensdata$comnum[projrows]),sensdata$vcount[projrows]/max(sensdata$vcount[projrows]),sensdata$modularity[projrows]/max(sensdata$modularity[projrows])))
projtype=append(projtype,c(rep("number of communities",length(projrows)),rep("number of vertices",length(projrows)),rep("modularity",length(projrows))))
projdisp=append(projdisp,rep(sensdata$dispth[projrows],3))
projyear=append(projyear,rep(as.character(year),3*length(projrows)))
}
g=ggplot(data.frame(dispth=projdisp,val=projval,objective=projtype,year=projyear))
g+geom_line(aes(x=dispth,y=val,col=objective,group=objective))+facet_wrap(~year)
g+geom_line(aes(x=dispth,y=val,col=objective,group=objective))+facet_wrap(~year)+
xlab("$\theta_c$")+ylab("normalized objectives")+ theme(axis.title = element_text(size = 22), axis.text.x = element_text(size = 15),   axis.text.y = element_text(size = 15))
g+geom_line(aes(x=dispth,y=val,col=objective,group=objective))+facet_wrap(~year)+
xlab(expression(theta_c))+ylab("normalized objectives")#+ theme(axis.title = element_text(size = 22), axis.text.x = element_text(size = 15),   axis.text.y = element_text(size = 15))
g+geom_line(aes(x=dispth,y=val,col=objective,group=objective))+facet_wrap(~year)+
xlab(expression(theta[1]_c))+ylab("normalized objectives")#+ theme(axis.title = element_text(size = 22), axis.text.x = element_text(size = 15),   axis.text.y = element_text(size = 15))
g+geom_line(aes(x=dispth,y=val,col=objective,group=objective))+facet_wrap(~year)+
xlab(expression(theta[1]))+ylab("normalized objectives")#+ theme(axis.title = element_text(size = 22), axis.text.x = element_text(size = 15),   axis.text.y = element_text(size = 15))
g+geom_line(aes(x=dispth,y=val,col=objective,group=objective))+facet_wrap(~year)+
xlab(expression(theta[c]))+ylab("normalized objectives")#+ theme(axis.title = element_text(size = 22), axis.text.x = element_text(size = 15),   axis.text.y = element_text(size = 15))
